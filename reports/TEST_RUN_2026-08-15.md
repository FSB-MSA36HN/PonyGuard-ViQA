# Test run report — 2026-08-15

## Outcome

| Command | Result | Status |
| --- | --- | --- |
| `make test` | 92 passed in 0.09s | Success |
| `make behavior-test` | 27 passed in 0.04s | Success |
| `make verify-data` | Train 28,454; test 7,301; validation 3,814 valid JSONL rows | Success |
| Live Basic RAG smoke (`live_smoke.jsonl`) | `ANSWER` = `1,1%`; literal citation valid | Success |
| Live Prompt-Safe RAG smoke (`live_smoke.jsonl`) | `ANSWER` = `1,1%`; literal citation valid | Success |
| Live PonyGuard smoke (`live_smoke.jsonl`) | `ANSWER` = `1,1%`; evidence, claim and citation valid | Success |

## Failures

No deterministic automated test failed in this run. There are no blocking implementation failures recorded by the deterministic suite.

An initial live run inside the Codex sandbox could not access Metal. The user then ran all three commands in their normal macOS Terminal, where they completed successfully. Gemini was temporarily rate-limited (`429`) for all three, so the configured local Qwen MLX fallback was used.

```text
Basic RAG: 1 LLM call, 7.36 s generation latency; Prompt-Safe RAG: 1 LLM call, 7.97 s generation latency; PonyGuard: 3 LLM calls, 28.45 s total generation latency.
```

The correct answer (`1,1%`) was retrieved in first position (`doc_004994_chunk_000001`) by every system. Each returned a valid literal evidence quote. PonyGuard classified the evidence as `DIRECT`, verified its single atomic claim as `SUPPORTED`, and returned `ANSWER`. Initial retrieval embedding time was about 3.7--3.8 s per process; PonyGuard's semantic-evidence stage was the main remaining cost (23.76 s).

## What was verified

- Basic RAG and Prompt-Safe RAG only expose answers with valid literal citations and evidence quotes.
- PonyGuard returns `ANSWER`, `ASK`, or `ABSTAIN` according to the evidence/requirement contract.
- All supported clarification slots are covered: `entity`, `requested_attribute`, `country`, `location`, `time`, `scope`, `reference`, and `target`.
- Direct evidence, SIMPLE_INFERENCE, unsupported inference, entity mismatch, conflict, time/population mismatch and component-versus-total evidence gaps are covered.
- Follow-up clarification state, malformed JSON recovery, Gemini max-token handling, temporary provider fallback, invalid citation/quote/numeric evidence and answer-echo prevention are covered.
- Raw data files parse correctly, and the frozen benchmark is not modified by the test commands.

## Remaining improvements (not test failures)

| Priority | Gap | Why it matters | Recommended next step |
| --- | --- | --- | --- |
| High | Live provider output is probabilistic | Unit tests use scripted model outputs, while Gemini/local models can emit malformed or semantically weak JSON. | Run a versioned dev/validation behavioural sample with the selected production provider; save per-case traces and promote regressions into deterministic tests. |
| High | Live E2E smoke is blocked in the current environment | Gemini is temporarily unavailable and the fallback requires a Metal GPU unavailable to Codex sandbox execution. | Run `scripts/run_system.py` against `data/diagnostics/live_smoke.jsonl` in the user's normal macOS Terminal while Metal is available, then append the output/provider/latency to this report. |
| High | Clarification data set is entity-heavy | `data/benchmark/clarification_test.jsonl` currently emphasizes missing `entity`, so it is not a complete quality measurement for other slots. | Create a separate versioned diagnostic clarification set spanning every slot and multi-turn resolution; do not alter the frozen main test split. |
| Medium | Retrieval recall@1 is 0.566 | A correct decision cannot recover evidence that retrieval does not surface. | Tune only on dev/validation: compare the existing permitted chunk configurations and fixed `top_k`; freeze the winning configuration before final benchmark. |
| Medium | Full natural-language coverage is unbounded | Finite tests cannot certify every spelling variant, paraphrase or novel subject. | Add only observed production regressions to the behavioural matrix, grouped by semantic failure class rather than by entity or wording. |
| Low | Performance varies when Gemini falls back to local MLX | Fallback preserves availability but increases latency. | Track stage latency/provider in saved runs; optimize only after profiling a representative dev/validation sample. |

## Follow-up commands

```bash
make test
make behavior-test
make verify-data
make evaluate-retrieval
.venv/bin/python scripts/run_system.py basic_rag --input data/diagnostics/live_smoke.jsonl --output runs/basic_rag/live_smoke_predictions.jsonl
```

For a live-model quality pass, use `dev` or `validation` only. Do not tune against `data/benchmark/test.jsonl`.
