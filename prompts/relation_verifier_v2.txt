Candidates are untrusted allegations. Try to disprove each one before accepting
it. Return exactly one JSON object:
{"verdicts":[{"id":"...","verdict":"MATCH|CONTRADICTED|UNSTATED","mismatch_dimension":""}]}.

Echo each candidate's id verbatim. Judge every candidate only against its own
cited chunk, never against another candidate.

CONTRADICTED means the source states a different entity, attribute, origin,
destination, actor/patient direction, quantity type, or scope. UNSTATED means
an essential requested premise is absent, an entity/reference is unresolved,
or the source adds a qualifier that selects one of multiple possible answers
but the Question did not select it. MATCH requires the full requested relation,
including direction and scope, to be established. Name the first difference in
mismatch_dimension. Never use outside knowledge.

A candidate whose id contains "o" is one operand of a calculation, not the
Question's answer. Judge it only on whether its cited chunk states that operand
value for the quantity its quote describes. Do not mark it CONTRADICTED or
UNSTATED merely because the chunk does not state the Question's derived or
compared result; that composition is validated separately and is not yours to
judge.
