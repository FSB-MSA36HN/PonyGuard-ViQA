Read the Question and retrieved Chunks. Return exactly one JSON object; never invent an answer outside the chunks.

Return semantic slots: entity, requested_attribute, answer_type (DATE|COUNT|PERSON|LOCATION|TIME|TEXT), question_clear, missing_requirements, ambiguity_type, clarification_options, clarification_question, population_scope, time_scope, inclusion_constraints. `missing_requirements` may contain only: entity, requested_attribute, country, location, time, scope, reference, target. Mark a slot as missing only when it is necessary to identify one answer. Do not ask for clarification when the question is already specific.

Use the question and the diversity of retrieved facts to decide whether a disambiguating slot is necessary. If valid-looking facts belong to different entities, places, times, populations, or other contexts, do not select one arbitrarily: name the missing schema slot and ask one concise question for it. Do not encode domain-specific rules, facts, or examples.

Return evidence fields: entity_match, attribute_match, conflict_detected, inference_level (DIRECT|SIMPLE_INFERENCE|UNSUPPORTED), reasoning_allowed, decision_rationale, coverage_gaps, support, retrieved_observations. `support` is an array of objects with chunk_id, evidence_quote, candidate_answer, support_type, evidence_time_scope, evidence_population_scope, matches_time, matches_population, mismatch_reasons. evidence_quote must be a contiguous verbatim excerpt from its chunk; candidate_answer must occur in the quote. Do not assume missing scope.

`retrieved_observations` is an array of at most two objects with chunk_id, evidence_quote, and limitation. Use it to report relevant retrieved material even when it cannot support the answer. The quote must be a contiguous verbatim excerpt from its chunk. State dynamically in `limitation` what the quote establishes and why it does not establish the requested answer; do not claim the corpus has no information. Do not put an observation in `support` unless it directly supports the requested answer.
