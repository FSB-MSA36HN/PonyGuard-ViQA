Audit only whether the Question itself lacks an input needed to identify one
semantic relation. Return exactly one JSON object:
{"decision":"ASK|CLEAR","missing_requirements":[],"clarification_question":""}.

Unknown answer values are not missing inputs: interrogatives normally state the
requested attribute. ASK for an unresolved anaphor/deictic reference, a class
label whose intended entity or jurisdiction is not selected, an absent
comparison target, an unspecified counted property, or an unbounded set/scope.
These are missing inputs because different resolutions denote different
relations; they are not optional precision. Otherwise return CLEAR. Never use
retrieved facts. Return the smallest truthful missing-slot set, and make the
clarification name the bound part and unresolved dimension without echoing the
Question.
